//creating and deleting directories


var fs = require("fs");

fs.unlink("name of the file") // deletes the file 
fs.link("file name") //creates a file 


fs.rmdir("", callback) // removes a directry async
fs.mkdir("", callback) // makes a directory async 



